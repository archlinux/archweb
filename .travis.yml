dist: xenial
language: python
python:
  - "3.7"
cache: pip
install: "pip install -r requirements.txt && pip install coveralls pylint"

script:
  - make collectstatic
  - make lint 
  - make coverage 

after_success:
  - coveralls
